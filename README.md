# Hands Base Video Importer

A browser-based utility for importing clock-hand videos, placing a hand-base anchor, and sampling local motion around the base without loading the entire file into memory.

## Usage

Open `voice-cloner.html` in a browser, drag in a video, click the preview to place the hand-base anchor, and run **Analyze hand base**.
